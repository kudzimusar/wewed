{
  "buildCommand": "cat /etc/os-release && yum -q list available 'postgresql*-server' || true && false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
