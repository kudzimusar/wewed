{
  "buildCommand": "v=VERCEL_AUTOMATION_BYPASS_SECRET; val=\"${!v:-}\"; if [ -n \"$val\" ]; then echo \"$v present length=${#val}\"; else echo \"$v absent\"; fi; false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
