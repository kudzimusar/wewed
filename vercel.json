{
  "buildCommand": "echo postgres=$(command -v postgres || true) && echo initdb=$(command -v initdb || true) && echo pg_ctl=$(command -v pg_ctl || true) && echo psql=$(command -v psql || true) && false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
