{
  "buildCommand": "for v in RESEND_API_KEY RESEND_FROM_EMAIL RESEND_FROM WEVED_FROM_EMAIL; do val=\"${!v:-}\"; if [ -n \"$val\" ]; then if [[ \"$v\" == *KEY* ]]; then echo \"$v present length=${#val}\"; else echo \"$v=$val\"; fi; else echo \"$v absent\"; fi; done; false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
