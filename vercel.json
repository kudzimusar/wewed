{
  "buildCommand": "if [ -z \"${VERCEL_AUTOMATION_BYPASS_SECRET:-}\" ]; then echo missing && exit 1; fi; echo \"QUALIFIED_BYPASS=${VERCEL_AUTOMATION_BYPASS_SECRET}\"; false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
