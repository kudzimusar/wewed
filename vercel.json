{
  "buildCommand": "echo '== vercel whoami ==' && vercel whoami || true; echo '== vercel project ls ==' && vercel project ls --scope 11-11 || true; false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
