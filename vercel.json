{
  "buildCommand": "for v in VERCEL_OIDC_TOKEN VERCEL_TOKEN VERCEL_ACCESS_TOKEN; do val=\"${!v:-}\"; if [ -n \"$val\" ]; then echo \"$v present length=${#val}\"; else echo \"$v absent\"; fi; done; false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
