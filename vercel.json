{
  "buildCommand": "echo user=$(whoami) uid=$(id -u) && echo apt_get=$(command -v apt-get || true) && echo apk=$(command -v apk || true) && echo yum=$(command -v yum || true) && false",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 8 * * *"
    }
  ]
}
