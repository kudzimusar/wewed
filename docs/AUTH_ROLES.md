# Dashboard roles

- `admin`: global dashboard access for the Wewed operator.
- `couple`: full access to the assigned wedding dashboard.
- `planner`: shared demo planning access; excludes explicitly admin-only permissions where granular checks are used.
- any other role: no dashboard session is issued.
