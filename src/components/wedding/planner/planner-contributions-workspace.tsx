'use client'

import { useCallback, useEffect, useMemo, useState } from 'react'
import { ArrowLeft, CircleDollarSign, Download, Gift, HandHeart, Loader2, NotebookPen, Plus, Search, Sparkles, Store, Users, X } from 'lucide-react'
import { useRouter } from 'next/navigation'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Checkbox } from '@/components/ui/checkbox'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { useToast } from '@/hooks/use-toast'
import { refreshPlannerWorksheet } from '@/lib/planner-workspace-events'
import { CONTRIBUTION_CAMPAIGN_TYPE_LABELS, CONTRIBUTION_TYPE_LABELS } from '@/lib/contributions'

type ContributionType = keyof typeof CONTRIBUTION_TYPE_LABELS
interface Contributor { id: string; displayName: string; legalName?: string | null; kind: string; email: string | null; phone: string | null; address: string | null; preferredContactMethod: string | null; relationship: string | null; publicRecognition: boolean; anonymousPublic?: boolean; notes?: string | null; history?: Array<{id:string;title:string;type:string;amount:number|null;currency:string;estimatedValue:number|null;estimatedValueCurrency:string|null;fulfillmentState:string;thankYouState:string;createdAt:string}> }
interface BudgetOption { id: string; description: string; category: string; currency: string; paidAmount: number; actualCost: number | null; estimatedCost: number; serviceEngagementId: string | null }
interface EngagementOption { id: string; serviceCategory: string; serviceDescription: string | null; currency: string; vendor: { id: string; name: string }; payments:Array<{id:string;amount:number;currency:string;paidAt:string|null;reference:string|null}> }
interface Allocation { id: string; budgetItemId: string; amount: number; currency: string; allocationKind: string; budgetItem: { id: string; description: string; category: string } }
interface Contribution {
  id: string; type: ContributionType; title: string; description: string | null; amount: number | null; directVendorPaidAmount: number; remainingAmount: number; currency: string; estimatedValue: number | null; estimatedValueCurrency: string | null; quantity: number | null; unit: string | null; route: string; commitmentState: string; fulfillmentState: string; verificationState: string; thankYouState: string; pledgedAt?: string | null; expectedAt?: string | null; fulfilledAt?: string | null; notes: string | null; contributor: Contributor; campaign: { id: string; title: string } | null; vendor: { id: string; name: string } | null; serviceEngagement: { id: string; serviceCategory: string | null; serviceDescription: string | null } | null; allocations: Allocation[]; availableAmount: number; allocatedAmount: number; taskLinks: Array<{ plannerTask: { id: string; title: string; status: string } }>;
}
interface Campaign { id: string; type: string; title: string; description: string | null; targetAmount: number | null; currency: string; published: boolean; showTarget: boolean; showRaised: boolean; externalUrl: string | null; ctaLabel: string | null; invitationVisible: boolean; showContributorRecognition:boolean; publicNote: string | null; publishFrom:string|null; publishUntil:string|null; raised: number }
interface CurrencySummary { currency: string; cashReceived: number; directVendorPaid: number; inKindValue: number; pledged: number; availableCash: number }
interface WorkspacePayload { success: boolean; weddingId: string; data: Contribution[]; contributors: Contributor[]; campaigns: Campaign[]; summaryByCurrency: CurrencySummary[]; counts: { contributors: number; pledged: number; toThank: number }; permissions: { canEdit: boolean; canCreateTasks: boolean }; options: { budgetItems: BudgetOption[]; engagements: EngagementOption[]; guests: Array<{id:string;name:string;email:string|null}>; vendors: Array<{id:string;name:string;category:string}> } }
interface FundingItem { id: string; description: string; paidAmount: number; currency: string; category: string; unattributed: number; funding: Array<{ sourceKind: string; contributionId: string | null; amount: number; currency: string }> }
interface EvidenceItem { id:string; vaultObjectId:string; displayName:string; mimeType:string; byteSize:number; storageState:string; scanState:string; createdAt:string }
interface PaymentFundingView { id:string; amount:number; currency:string; paidAt:string|null; reference:string|null; vendor:{id:string;name:string}; serviceEngagementId:string; budgetItems:Array<{id:string;description:string;currency:string}>; funding:Array<{id:string;sourceKind:string;contributionId:string|null;budgetItemId:string|null;amount:number;currency:string}>; unattributed:number }

const INITIAL_FORM = { contributorId: '', contributorName: '', contributorKind: 'individual', email: '', phone: '', address: '', preferredContactMethod: '', publicRecognition: false, relationship: '', guestId: '', expectedAt: '', type: 'CASH_TO_COUPLE' as ContributionType, title: '', amount: '', currency: 'USD', estimatedValue: '', quantity: '', unit: '', status: 'RECEIVED', budgetItemId: '', serviceEngagementId: '', campaignId: '', paymentReference: '', alreadyIncludedInBudgetPaid: false, notes: '' }

function money(value: number, currency = 'USD') { try { return new Intl.NumberFormat('en-US', { style: 'currency', currency, maximumFractionDigits: 2 }).format(value) } catch { return `${currency} ${value.toFixed(2)}` } }
function human(value: string) { return value.toLowerCase().replaceAll('_',' ').replace(/\b\w/g, (letter) => letter.toUpperCase()) }
function contributionTypeText(item: Pick<Contribution, 'type' | 'fulfillmentState'>) { if (item.type !== 'DIRECT_VENDOR_PAYMENT') return CONTRIBUTION_TYPE_LABELS[item.type]; if (item.fulfillmentState === 'PAID_DIRECT') return 'Paid vendor directly'; if (item.fulfillmentState === 'PARTIALLY_RECEIVED') return 'Part-paid vendor directly'; return 'To pay vendor directly' }
function Panel({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-2xl border border-gold/15 bg-champagne/[0.035] ${className}`}>{children}</section> }

export function PlannerContributionsWorkspace({ embedded = false }: { embedded?: boolean } = {}) {
  const router = useRouter()
  const { toast } = useToast()
  const [workspace, setWorkspace] = useState<WorkspacePayload | null>(null)
  const [funding, setFunding] = useState<FundingItem[]>([])
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const [search, setSearch] = useState('')
  const [filter, setFilter] = useState('all')
  const [addOpen, setAddOpen] = useState(false)
  const [manage, setManage] = useState<Contribution | null>(null)
  const [form, setForm] = useState(INITIAL_FORM)
  const [allocation, setAllocation] = useState({ budgetItemId: '', amount: '' })
  const [taskTitle, setTaskTitle] = useState('')
  const [noteText, setNoteText] = useState('')
  const [fundingSelections, setFundingSelections] = useState<Record<string,string>>({})
  const [fundingAmounts, setFundingAmounts] = useState<Record<string,string>>({})
  const [directPaymentReference, setDirectPaymentReference] = useState('')
  const [directPaymentAmount, setDirectPaymentAmount] = useState('')
  const [contributorProfile, setContributorProfile] = useState<Contributor | null>(null)
  const [contributorEdit, setContributorEdit] = useState({ displayName:'', legalName:'', kind:'individual', relationship:'', email:'', phone:'', address:'', preferredContactMethod:'', publicRecognition:false, anonymousPublic:false, notes:'' })
  const [contributionEdit, setContributionEdit] = useState({ title:'', description:'', notes:'', expectedAt:'', amount:'', estimatedValue:'', quantity:'', unit:'' })
  const [evidence, setEvidence] = useState<EvidenceItem[]>([])
  const [evidenceFile, setEvidenceFile] = useState<File | null>(null)
  const [paymentFundingView, setPaymentFundingView] = useState<PaymentFundingView | null>(null)
  const [paymentFundingForm, setPaymentFundingForm] = useState({ sourceKind:'COUPLE', amount:'', contributionId:'', budgetItemId:'' })
  const [giftingShare, setGiftingShare] = useState<{url:string;qr:string}|null>(null)
  const [aiDraft, setAiDraft] = useState<{kind:'thank'|'task';text:string}|null>(null)
  const [campaignForm, setCampaignForm] = useState({ type: 'HONEYMOON', title: '', description: '', targetAmount: '', currency: 'USD', externalUrl: '' })

  const load = useCallback(async (spinner = false) => {
    if (spinner) setLoading(true)
    setError('')
    try {
      const [contributionResponse, fundingResponse] = await Promise.all([
        fetch('/api/planner/contributions', { cache: 'no-store' }),
        fetch('/api/planner/budget/funding', { cache: 'no-store' }),
      ])
      const contributionBody = await contributionResponse.json()
      if (!contributionResponse.ok || contributionBody.success === false) throw new Error(contributionBody.error || 'Could not load contributions.')
      setWorkspace(contributionBody)
      if (fundingResponse.ok) {
        const fundingBody = await fundingResponse.json()
        if (fundingBody.success !== false) setFunding(fundingBody.data ?? [])
      }
    } catch (reason) {
      setError(reason instanceof Error ? reason.message : 'Could not load contributions.')
    } finally { setLoading(false) }
  }, [])

  useEffect(() => { void load(true) }, [load])

  const canEdit = workspace?.permissions?.canEdit === true
  const canCreateTasks = workspace?.permissions?.canCreateTasks === true

  const filtered = useMemo(() => {
    const query = search.trim().toLowerCase()
    return (workspace?.data ?? []).filter((item) => {
      if (filter === 'pledged' && !(item.commitmentState === 'PLEDGED' && ['PENDING','PARTIALLY_RECEIVED'].includes(item.fulfillmentState))) return false
      if (filter === 'received' && !['RECEIVED','DELIVERED','PARTIALLY_RECEIVED','PAID_DIRECT','COMPLETED'].includes(item.fulfillmentState)) return false
      if (filter === 'direct' && item.type !== 'DIRECT_VENDOR_PAYMENT') return false
      if (filter === 'in-kind' && !['GOODS_IN_KIND','SERVICE_IN_KIND','TIME_LABOUR','DISCOUNT_SPONSORSHIP'].includes(item.type)) return false
      if (filter === 'thank' && !['TO_THANK','PREPARED'].includes(item.thankYouState)) return false
      if (filter === 'overdue' && !(item.expectedAt && new Date(item.expectedAt).getTime() < Date.now() && ['PENDING','PARTIALLY_RECEIVED'].includes(item.fulfillmentState))) return false
      if (filter === 'unverified' && !(item.verificationState === 'UNVERIFIED' && ['RECEIVED','DELIVERED','PAID_DIRECT','COMPLETED'].includes(item.fulfillmentState))) return false
      return !query || [item.contributor.displayName,item.title,item.description ?? '',item.vendor?.name ?? '',item.campaign?.title ?? ''].some((value) => value.toLowerCase().includes(query))
    })
  }, [workspace, search, filter])

  async function mutate(url: string, init: RequestInit) {
    setSaving(true); setError('')
    try {
      const response = await fetch(url, init)
      const body = await response.json()
      if (!response.ok || body.success === false) throw new Error(body.error || 'The change could not be saved.')
      await load(false)
      refreshPlannerWorksheet()
      return true
    } catch (reason) {
      const message = reason instanceof Error ? reason.message : 'The change could not be saved.'
      setError(message); toast({ title: 'Could not save', description: message, variant: 'destructive' }); return false
    } finally { setSaving(false) }
  }

  async function addContribution(event: React.FormEvent) {
    event.preventDefault()
    const inKind = ['GOODS_IN_KIND','SERVICE_IN_KIND','TIME_LABOUR','DISCOUNT_SPONSORSHIP'].includes(form.type)
    const direct = form.type === 'DIRECT_VENDOR_PAYMENT'
    const fulfillmentState = form.status === 'PROMISED' ? 'PENDING' : direct ? 'PAID_DIRECT' : inKind ? 'DELIVERED' : 'RECEIVED'
    const success = await mutate('/api/planner/contributions', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({
      contributorId: form.contributorId || undefined,
      contributor: form.contributorId ? undefined : { displayName: form.contributorName, kind: form.contributorKind, email: form.email, phone: form.phone, address: form.address, preferredContactMethod: form.preferredContactMethod || null, publicRecognition: form.publicRecognition, relationship: form.relationship, guestId: form.guestId || null },
      type: form.type, title: form.title, amount: form.amount ? Number(form.amount) : null, currency: form.currency,
      estimatedValue: form.estimatedValue ? Number(form.estimatedValue) : null, estimatedValueCurrency: form.currency,
      quantity: form.quantity ? Number(form.quantity) : null, unit: form.unit || null,
      commitmentState: form.status === 'PROMISED' ? 'PLEDGED' : 'NOT_APPLICABLE', fulfillmentState, expectedAt: form.expectedAt || null,
      budgetItemId: form.budgetItemId || null, serviceEngagementId: form.serviceEngagementId || null,
      campaignId: form.campaignId || null, paymentReference: form.paymentReference || null,
      alreadyIncludedInBudgetPaid: form.alreadyIncludedInBudgetPaid, notes: form.notes,
    }) })
    if (success) { setAddOpen(false); setForm(INITIAL_FORM); toast({ title: 'Contribution saved', description: 'The help is recorded separately from what you personally paid.' }) }
  }

  async function contributionAction(action: string, extra: Record<string, unknown> = {}) {
    if (!manage) return
    const success = await mutate(`/api/planner/contributions/${manage.id}/actions`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ action, ...extra }) })
    if (success) setManage(null)
  }

  async function loadContributionDetails(item: Contribution) {
    setManage(item)
    setDirectPaymentReference('')
    setDirectPaymentAmount('')
    setTaskTitle(`Follow up contribution from ${item.contributor.displayName}`)
    setContributionEdit({ title:item.title, description:item.description ?? '', notes:item.notes ?? '', expectedAt:item.expectedAt?.slice(0,10) ?? '', amount:item.amount === null ? '' : String(item.amount), estimatedValue:item.estimatedValue === null ? '' : String(item.estimatedValue), quantity:item.quantity === null ? '' : String(item.quantity), unit:item.unit ?? '' })
    setEvidence([])
    setContributorProfile(null)
    try {
      const [personResponse,evidenceResponse] = await Promise.all([
        fetch(`/api/planner/contributors/${item.contributor.id}`, { cache:'no-store' }),
        fetch(`/api/planner/contributions/${item.id}/evidence`, { cache:'no-store' }),
      ])
      if (personResponse.ok) {
        const personBody = await personResponse.json()
        if (personBody.success !== false && personBody.data) {
          setContributorProfile(personBody.data)
          setContributorEdit({ displayName:personBody.data.displayName ?? '', legalName:personBody.data.legalName ?? '', kind:personBody.data.kind ?? 'individual', relationship:personBody.data.relationship ?? '', email:personBody.data.email ?? '', phone:personBody.data.phone ?? '', address:personBody.data.address ?? '', preferredContactMethod:personBody.data.preferredContactMethod ?? '', publicRecognition:personBody.data.publicRecognition === true, anonymousPublic:personBody.data.anonymousPublic === true, notes:personBody.data.notes ?? '' })
        }
      }
      if (evidenceResponse.ok) {
        const evidenceBody = await evidenceResponse.json()
        if (evidenceBody.success !== false) setEvidence(evidenceBody.data ?? [])
      }
    } catch { /* Detail enrichment must not hide the core contribution record. */ }
  }

  async function saveContributorProfile() {
    if (!manage || !canEdit) return
    const success = await mutate(`/api/planner/contributors/${manage.contributor.id}`, { method:'PATCH', headers:{'content-type':'application/json'}, body:JSON.stringify(contributorEdit) })
    if (success) {
      const response = await fetch(`/api/planner/contributors/${manage.contributor.id}`, { cache:'no-store' })
      if (response.ok) { const body = await response.json(); if (body.data) setContributorProfile(body.data) }
      toast({ title:'Contributor updated' })
    }
  }

  async function saveContributionDetails() {
    if (!manage || !canEdit) return
    const success = await mutate(`/api/planner/contributions/${manage.id}`, { method:'PATCH', headers:{'content-type':'application/json'}, body:JSON.stringify({ title:contributionEdit.title, description:contributionEdit.description, notes:contributionEdit.notes, expectedAt:contributionEdit.expectedAt || null, ...(manage.type === 'DIRECT_VENDOR_PAYMENT' && manage.fulfillmentState === 'PENDING' ? { amount:Number(contributionEdit.amount) } : {}), estimatedValue:contributionEdit.estimatedValue === '' ? null : Number(contributionEdit.estimatedValue), quantity:contributionEdit.quantity === '' ? null : Number(contributionEdit.quantity), unit:contributionEdit.unit || null }) })
    if (success) { setManage(null); toast({ title:'Contribution updated' }) }
  }

  async function attachEvidence() {
    if (!manage || !canEdit || !evidenceFile) return
    setSaving(true)
    try {
      const formData = new FormData(); formData.set('file', evidenceFile)
      const response = await fetch(`/api/planner/contributions/${manage.id}/evidence`, { method:'POST', body:formData })
      const body = await response.json()
      if (!response.ok || body.success === false) throw new Error(body.error || 'Could not attach evidence.')
      setEvidenceFile(null)
      const refresh = await fetch(`/api/planner/contributions/${manage.id}/evidence`, { cache:'no-store' })
      const refreshed = await refresh.json(); if (refresh.ok && refreshed.success !== false) setEvidence(refreshed.data ?? [])
      toast({ title:'Evidence attached', description:'Stored in the governed Wewed Vault.' })
    } catch (reason) { toast({ title:'Evidence upload failed', description:reason instanceof Error ? reason.message : undefined, variant:'destructive' }) }
    finally { setSaving(false) }
  }

  async function openEvidence(item: EvidenceItem) {
    const response = await fetch(`/api/vault/${item.vaultObjectId}`, { cache:'no-store' })
    const body = await response.json()
    if (!response.ok || body.success === false || !body.data?.signedUrl) { toast({ title:'Evidence unavailable', description:body.error, variant:'destructive' }); return }
    window.open(body.data.signedUrl, '_blank', 'noopener,noreferrer')
  }

  async function unlinkEvidence(item: EvidenceItem) {
    if (!manage || !canEdit) return
    const success = await mutate(`/api/planner/contributions/${manage.id}/evidence`, { method:'DELETE', headers:{'content-type':'application/json'}, body:JSON.stringify({ linkId:item.id }) })
    if (success) setEvidence((current) => current.filter((candidate) => candidate.id !== item.id))
  }

  async function loadPaymentFunding(paymentId:string) {
    const response = await fetch(`/api/planner/payments/${paymentId}/funding`, { cache:'no-store' })
    const body = await response.json()
    if (!response.ok || body.success === false) { toast({ title:'Could not load payment funding', description:body.error, variant:'destructive' }); return }
    setPaymentFundingView(body.data)
    setPaymentFundingForm({ sourceKind:'COUPLE', amount:String(body.data.unattributed || ''), contributionId:'', budgetItemId:body.data.budgetItems?.[0]?.id ?? '' })
  }

  async function savePaymentFunding() {
    if (!paymentFundingView || !canEdit) return
    const amount = Number(paymentFundingForm.amount)
    if (!Number.isFinite(amount) || amount <= 0) { toast({ title:'Enter a positive funding amount', variant:'destructive' }); return }
    const success = await mutate(`/api/planner/payments/${paymentFundingView.id}/funding`, { method:'POST', headers:{'content-type':'application/json'}, body:JSON.stringify({ sourceKind:paymentFundingForm.sourceKind, amount, contributionId:paymentFundingForm.sourceKind==='CONTRIBUTION'?paymentFundingForm.contributionId:null, budgetItemId:paymentFundingForm.budgetItemId || null }) })
    if (success) await loadPaymentFunding(paymentFundingView.id)
  }

  async function draftWithAi(action:'draft-thank-you'|'draft-follow-up-task') {
    if(!manage)return
    setSaving(true)
    try {
      const response=await fetch(`/api/planner/contributions/${manage.id}/assist`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action})})
      const body=await response.json(); if(!response.ok||body.success===false)throw new Error(body.error||'AI draft unavailable.')
      setAiDraft({kind:action==='draft-thank-you'?'thank':'task',text:body.data.draft})
    } catch(reason){toast({title:'AI draft unavailable',description:reason instanceof Error?reason.message:undefined,variant:'destructive'})}
    finally{setSaving(false)}
  }

  async function createNotebookNote() {
    if (!manage || !workspace || !noteText.trim()) return
    setSaving(true)
    try {
      const response = await fetch('/api/notebook', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ title: `Contribution — ${manage.contributor.displayName}`, contentText: noteText.trim(), weddingId: workspace.weddingId, noteType: 'QUICK', visibility: 'PRIVATE', contextType: 'contribution' }) })
      const note = await response.json()
      if (!response.ok || note.success === false || !note.data?.id) throw new Error(note.error || 'Could not create Notebook note.')
      const linkResponse = await fetch(`/api/notebook/${note.data.id}/actions`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ action: 'add-link', entityType: 'WeddingContribution', entityId: manage.id, labelSnapshot: `${manage.contributor.displayName} — ${manage.title}` }) })
      const link = await linkResponse.json()
      if (!linkResponse.ok || link.success === false) throw new Error(link.error || 'The note was created but could not be linked.')
      setNoteText(''); toast({ title: 'Notebook note linked' })
    } catch (reason) { toast({ title: 'Notebook link failed', description: reason instanceof Error ? reason.message : undefined, variant: 'destructive' }) }
    finally { setSaving(false) }
  }

  async function createCampaign(event: React.FormEvent) {
    event.preventDefault()
    const success = await mutate('/api/planner/contribution-campaigns', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ type: campaignForm.type, title: campaignForm.title, description: campaignForm.description, targetAmount: campaignForm.targetAmount ? Number(campaignForm.targetAmount) : null, currency: campaignForm.currency, externalUrl: campaignForm.externalUrl || null }) })
    if (success) setCampaignForm({ type: 'HONEYMOON', title: '', description: '', targetAmount: '', currency: 'USD', externalUrl: '' })
  }

  async function patchCampaign(campaign: Campaign, patch: Record<string, unknown>) {
    await mutate(`/api/planner/contribution-campaigns/${campaign.id}`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify(patch) })
  }

  async function prepareGiftingShare() {
    try {
      const response = await fetch('/api/planner/contribution-campaigns/share', { cache:'no-store' })
      const body = await response.json()
      if (!response.ok || body.success === false) throw new Error(body.error || 'Could not create gifting link.')
      const url = `${window.location.origin}${body.data.path}`
      const qrResponse = await fetch(`/api/qrcode?data=${encodeURIComponent(url)}&size=260`, { cache:'no-store' })
      const qrBody = await qrResponse.json()
      if (!qrResponse.ok || !qrBody.qr) throw new Error(qrBody.error || 'Could not create gifting QR code.')
      setGiftingShare({ url, qr:qrBody.qr })
      await navigator.clipboard?.writeText(url).catch(()=>undefined)
      toast({ title:'Gifting link ready', description:'The public Registry link has also been copied when browser permissions allow.' })
    } catch (reason) { toast({ title:'Gifting link unavailable', description:reason instanceof Error ? reason.message : undefined, variant:'destructive' }) }
  }

  async function classifyFunding(item: FundingItem, sourceKind: 'COUPLE' | 'CONTRIBUTION') {
    if (item.unattributed <= 0) return
    const amount = Number(fundingAmounts[item.id] ?? item.unattributed)
    const contributionId = fundingSelections[item.id] ?? ''
    if (!Number.isFinite(amount) || amount <= 0 || amount > item.unattributed + 0.0001) { toast({ title: 'Enter an amount within the source-not-recorded balance', variant: 'destructive' }); return }
    if (sourceKind === 'CONTRIBUTION' && !contributionId) { toast({ title: 'Choose received contribution cash first', variant: 'destructive' }); return }
    const success = await mutate('/api/planner/budget/funding', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ budgetItemId: item.id, sourceKind, amount, contributionId: sourceKind === 'CONTRIBUTION' ? contributionId : null }) })
    if (success) { setFundingSelections((current) => ({...current,[item.id]:''})); setFundingAmounts((current) => ({...current,[item.id]:''})) }
  }

  if (loading && !workspace) return <div className="flex min-h-[60dvh] items-center justify-center bg-espresso text-gold"><Loader2 className="size-8 animate-spin" /></div>

  return <div className={embedded ? 'text-champagne' : 'min-h-screen bg-espresso text-champagne'} data-testid="planner-contributions-workspace">
    {!embedded && <header className="sticky top-0 z-40 border-b border-gold/15 bg-espresso/95 px-3 py-2 backdrop-blur sm:px-5"><div className="mx-auto flex max-w-7xl items-center gap-3"><Button variant="ghost" size="sm" onClick={() => router.push('/planner/overview')} className="text-champagne/70"><ArrowLeft className="size-4" />Planner</Button><div className="min-w-0 flex-1"><p className="truncate text-[10px] uppercase tracking-[0.18em] text-gold">Contributions</p><p className="hidden text-xs text-champagne/40 sm:block">Money, vendor payments, goods, services and time</p></div><Button asChild variant="outline" size="sm" className="border-gold/20 bg-transparent text-champagne/70"><a href="/api/planner/contributions/export"><Download className="size-4" /><span className="hidden sm:inline">Export</span></a></Button>{canEdit && <Button size="sm" onClick={() => setAddOpen(true)} className="bg-gold text-espresso hover:bg-gold-light"><Plus className="size-4" />Add</Button>}</div></header>}

    <main className={embedded ? 'space-y-5' : 'mx-auto max-w-7xl space-y-5 px-3 py-5 sm:px-5 sm:py-7'}>
      {embedded && <div className="flex flex-wrap items-start justify-between gap-3 border-b border-gold/10 pb-4"><div><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-gold">Contributions</p><p className="mt-1 text-xs text-champagne/45">Money, vendor payments, goods, services and time</p></div><div className="flex gap-2"><Button asChild variant="outline" size="sm" className="border-gold/20 bg-transparent text-champagne/70"><a href="/api/planner/contributions/export"><Download className="size-4" /><span className="hidden sm:inline">Export</span></a></Button>{canEdit && <Button size="sm" onClick={() => setAddOpen(true)} className="bg-gold text-espresso hover:bg-gold-light"><Plus className="size-4" />Add</Button>}</div></div>}
      {error && <div role="alert" className="rounded-xl border border-clay/30 bg-clay/10 px-4 py-3 text-sm text-clay-light">{error}</div>}
      <div><h1 className="font-serif text-2xl sm:text-3xl">Who helped make this possible?</h1><p className="mt-1 max-w-2xl text-sm leading-6 text-champagne/55">Record support in everyday language. Wewed keeps “paid” separate from “paid by us” so your budget tells the truth.</p></div>

      <div className="grid grid-cols-2 gap-2 lg:grid-cols-4">
        {(workspace?.summaryByCurrency ?? []).length === 0 ? (
          <Panel className="col-span-2 p-4 lg:col-span-4">
            <p className="text-sm text-champagne/55">No contributions yet. Add the first person, family or organisation that helped.</p>
          </Panel>
        ) : (
          workspace!.summaryByCurrency.flatMap((summary) => {
            const cards = [
              { label: 'Money received', value: summary.cashReceived, detail: 'Cash given to you', Icon: CircleDollarSign },
              { label: 'Paid direct', value: summary.directVendorPaid, detail: 'Paid straight to a vendor', Icon: Store },
              { label: 'In-kind value', value: summary.inKindValue, detail: 'Estimated non-cash help', Icon: Gift },
              { label: 'Still available', value: summary.availableCash, detail: 'Received cash not yet allocated', Icon: HandHeart },
            ]
            return cards.map(({ label, value, detail, Icon }) => (
              <Panel key={`${summary.currency}-${label}`} className="p-3 sm:p-4">
                <div className="flex items-center justify-between text-gold">
                  <p className="text-[9px] uppercase tracking-[0.12em] sm:text-[10px]">{label}</p>
                  <Icon className="size-4" />
                </div>
                <p className="mt-2 font-serif text-xl sm:text-2xl">{money(value, summary.currency)}</p>
                <p className="mt-1 text-[10px] leading-4 text-champagne/45">{detail} · {summary.currency}</p>
              </Panel>
            ))
          })
        )}
      </div>
      {(workspace?.summaryByCurrency ?? []).some((summary) => summary.pledged > 0) && <Panel className="p-3"><p className="text-xs text-champagne/60"><span className="font-semibold text-gold">Promised, not received:</span> {workspace!.summaryByCurrency.filter((item) => item.pledged > 0).map((item) => money(item.pledged,item.currency)).join(' · ')}. Promises never count as cash received.</p></Panel>}

      <Panel className="overflow-hidden"><div className="grid gap-2 border-b border-gold/10 p-3 sm:grid-cols-[1fr_13rem]"><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-champagne/35" /><Input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search contributor, item, vendor or campaign" className="border-gold/20 bg-espresso/70 pl-9" /></div><select aria-label="Filter contributions" value={filter} onChange={(event) => setFilter(event.target.value)} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="all">Everything</option><option value="pledged">Promised</option><option value="received">Received / delivered</option><option value="direct">Direct vendor support</option><option value="in-kind">Goods / services / time</option><option value="overdue">Promised / overdue</option><option value="unverified">Received but unverified</option><option value="thank">Needs a thank-you</option></select></div>
        {filtered.length === 0 ? <div className="p-8 text-center text-sm text-champagne/50">No contributions in this view.</div> : <><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-gold/10 text-[10px] uppercase tracking-wider text-champagne/45"><tr><th className="p-3">Contributor</th><th className="p-3">Help</th><th className="p-3">Value</th><th className="p-3">State</th><th className="p-3">Where it went</th><th className="p-3"></th></tr></thead><tbody>{filtered.map((item) => <tr key={item.id} className="border-b border-gold/10 last:border-0"><td className="p-3"><p className="font-medium">{item.contributor.displayName}</p><p className="text-xs text-champagne/45">{item.contributor.relationship || 'Contributor'}</p></td><td className="p-3"><p>{item.title}</p><p className="text-xs text-champagne/45">{contributionTypeText(item)}</p></td><td className="p-3">{item.amount !== null ? money(item.amount,item.currency) : item.estimatedValue !== null ? `${money(item.estimatedValue,item.estimatedValueCurrency || item.currency)} est.` : 'Not valued'}</td><td className="p-3"><Badge variant="outline" className="border-gold/25 text-champagne/75">{human(item.fulfillmentState)}</Badge></td><td className="p-3 text-champagne/60">{item.allocations[0]?.budgetItem.description || item.vendor?.name || item.campaign?.title || (item.availableAmount > 0 ? `${money(item.availableAmount,item.currency)} still available` : 'Not allocated')}</td><td className="p-3 text-right"><Button size="sm" variant="outline" onClick={() => { void loadContributionDetails(item) }} className="border-gold/20 bg-transparent">Manage</Button></td></tr>)}</tbody></table></div><div className="space-y-2 p-3 md:hidden">{filtered.map((item) => <button key={item.id} type="button" onClick={() => { void loadContributionDetails(item) }} className="w-full rounded-xl border border-gold/12 bg-espresso/45 p-3 text-left"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{item.contributor.displayName}</p><p className="mt-0.5 text-xs text-champagne/50">{item.title}</p></div><p className="shrink-0 font-serif text-base text-gold">{item.amount !== null ? money(item.amount,item.currency) : item.estimatedValue !== null ? `${money(item.estimatedValue,item.estimatedValueCurrency || item.currency)} est.` : '—'}</p></div><div className="mt-3 flex flex-wrap gap-2"><Badge variant="outline" className="border-gold/25 text-[10px] text-champagne/70">{human(item.fulfillmentState)}</Badge><Badge variant="outline" className="border-champagne/15 text-[10px] text-champagne/55">{contributionTypeText(item)}</Badge></div></button>)}</div></>}
      </Panel>

      {(workspace?.options.engagements ?? []).some((engagement) => engagement.payments?.length) && <Panel className="p-4"><div className="flex items-start gap-3"><CircleDollarSign className="mt-0.5 size-5 text-gold"/><div><h2 className="font-serif text-xl">Payment funding</h2><p className="mt-1 text-xs leading-5 text-champagne/50">A real vendor payment can be split between couple funds and received contribution cash. The payment amount stays unchanged; these rows only explain who funded it.</p></div></div><div className="mt-4 grid gap-2 lg:grid-cols-2">{workspace!.options.engagements.flatMap((engagement)=>engagement.payments.map((payment)=><button type="button" key={payment.id} onClick={()=>void loadPaymentFunding(payment.id)} className="rounded-xl border border-gold/12 p-3 text-left hover:border-gold/30"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{engagement.vendor.name}</p><p className="text-xs text-champagne/45">{engagement.serviceCategory} · {payment.reference || 'Payment record'}</p></div><span className="font-serif text-gold">{money(Number(payment.amount),payment.currency)}</span></div></button>))}</div>{paymentFundingView && <div className="mt-4 rounded-xl border border-gold/20 bg-gold/[0.035] p-3"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="font-medium">{paymentFundingView.vendor.name} · {money(paymentFundingView.amount,paymentFundingView.currency)}</p><p className="text-xs text-champagne/45">Unattributed: {money(paymentFundingView.unattributed,paymentFundingView.currency)}</p></div><Button size="sm" variant="ghost" onClick={()=>setPaymentFundingView(null)}>Close</Button></div><div className="mt-3 space-y-1">{paymentFundingView.funding.filter((row)=>row.sourceKind!=='LEGACY_UNATTRIBUTED').map((row)=><div key={row.id} className="flex justify-between text-xs text-champagne/60"><span>{human(row.sourceKind)}</span><span>{money(row.amount,row.currency)}</span></div>)}</div>{canEdit && paymentFundingView.unattributed > 0 && <div className="mt-3 grid gap-2 sm:grid-cols-4"><select value={paymentFundingForm.sourceKind} onChange={(e)=>setPaymentFundingForm((c)=>({...c,sourceKind:e.target.value,contributionId:''}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-2 text-xs"><option value="COUPLE">Couple funds</option><option value="CONTRIBUTION">Contribution cash</option><option value="OTHER">Other known source</option></select><Input inputMode="decimal" value={paymentFundingForm.amount} onChange={(e)=>setPaymentFundingForm((c)=>({...c,amount:e.target.value}))} placeholder="Amount" className="border-gold/20 bg-espresso/70"/>{paymentFundingForm.sourceKind==='CONTRIBUTION' ? <select value={paymentFundingForm.contributionId} onChange={(e)=>setPaymentFundingForm((c)=>({...c,contributionId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-2 text-xs"><option value="">Choose received contribution</option>{workspace!.data.filter((item)=>['CASH_TO_COUPLE','HONEYMOON_GIFT'].includes(item.type) && item.fulfillmentState==='RECEIVED' && item.currency===paymentFundingView.currency && item.availableAmount>0).map((item)=><option key={item.id} value={item.id}>{item.contributor.displayName} — {item.title}</option>)}</select> : <select value={paymentFundingForm.budgetItemId} onChange={(e)=>setPaymentFundingForm((c)=>({...c,budgetItemId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-2 text-xs"><option value="">No Budget item</option>{paymentFundingView.budgetItems.map((item)=><option key={item.id} value={item.id}>{item.description}</option>)}</select>}{paymentFundingForm.sourceKind==='CONTRIBUTION' && <select value={paymentFundingForm.budgetItemId} onChange={(e)=>setPaymentFundingForm((c)=>({...c,budgetItemId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-2 text-xs"><option value="">No Budget item</option>{paymentFundingView.budgetItems.map((item)=><option key={item.id} value={item.id}>{item.description}</option>)}</select>}<Button disabled={saving || (paymentFundingForm.sourceKind==='CONTRIBUTION' && !paymentFundingForm.contributionId)} onClick={()=>void savePaymentFunding()} className="bg-gold text-espresso sm:col-span-4">Add funding split</Button></div>}</div>}</Panel>}

      {canEdit && funding.some((item) => item.unattributed > 0) && <Panel className="p-4"><div className="flex items-start gap-3"><CircleDollarSign className="mt-0.5 size-5 text-gold" /><div><h2 className="font-serif text-xl">Previous payments to classify</h2><p className="mt-1 text-xs leading-5 text-champagne/50">These amounts were already marked Paid before Wewed tracked who funded them. Classify all or part of each paid amount without changing the payment itself.</p></div></div><div className="mt-4 grid gap-2 lg:grid-cols-2">{funding.filter((item) => item.unattributed > 0).map((item) => { const eligible = (workspace?.data ?? []).filter((contribution) => contribution.currency === item.currency && ['CASH_TO_COUPLE','HONEYMOON_GIFT'].includes(contribution.type) && contribution.fulfillmentState === 'RECEIVED' && contribution.availableAmount > 0); const selected = fundingSelections[item.id] ?? ''; const amount = fundingAmounts[item.id] ?? String(item.unattributed); return <div key={item.id} className="rounded-xl border border-gold/12 p-3"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{item.description}</p><p className="text-xs text-champagne/45">Already paid: {money(item.paidAmount,item.currency)}</p></div><Badge variant="outline" className="border-clay/30 text-clay-light">{money(item.unattributed,item.currency)} source not recorded</Badge></div><div className="mt-3 grid gap-2 sm:grid-cols-[8rem_auto_minmax(12rem,1fr)_auto]"><Input aria-label={`Funding amount ${item.description}`} inputMode="decimal" value={amount} onChange={(event)=>setFundingAmounts((current)=>({...current,[item.id]:event.target.value}))} placeholder="Amount" className="h-9 border-gold/20 bg-espresso/70 text-xs"/><Button size="sm" variant="outline" disabled={saving} onClick={() => void classifyFunding(item,'COUPLE')} className="border-gold/20 bg-transparent">Paid by us</Button><select aria-label={`Contribution funding ${item.description}`} value={selected} disabled={eligible.length === 0} onChange={(event) => setFundingSelections((current)=>({...current,[item.id]:event.target.value}))} className="h-9 min-w-44 rounded-md border border-gold/20 bg-espresso px-2 text-xs"><option value="">{eligible.length ? 'Choose received contribution cash…' : 'No received contribution cash available'}</option>{eligible.map((contribution) => <option key={contribution.id} value={contribution.id}>{contribution.contributor.displayName} — {contribution.title} ({money(contribution.availableAmount,contribution.currency)} available)</option>)}</select><Button size="sm" variant="outline" disabled={saving || !selected} onClick={() => void classifyFunding(item,'CONTRIBUTION')} className="border-gold/20 bg-transparent">Paid by contributor</Button></div><p className="mt-2 text-[10px] leading-4 text-champagne/40">“Choose contribution” means cash already received by you and still available to fund this past payment. A promise to pay a vendor is not cash received and will not appear here.</p></div>})}</div></Panel>}

      <Panel className="p-4"><div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.15fr)]"><div><p className="text-[10px] uppercase tracking-[0.16em] text-gold">Honeymoon & gifting</p><h2 className="mt-1 font-serif text-xl">Optional information, never a begging page</h2><p className="mt-1 text-xs leading-5 text-champagne/50">Create it privately first. Publication, invitation visibility, target and progress are separate choices. Contributor names and individual amounts stay private.</p>{workspace?.campaigns.length ? <div className="mt-4 space-y-2">{workspace.campaigns.map((campaign) => <div key={campaign.id} className="rounded-xl border border-gold/12 p-3"><div className="flex items-start justify-between gap-2"><div><p className="font-medium">{campaign.title}</p><p className="text-xs text-champagne/45">{campaign.raised > 0 ? `${money(campaign.raised,campaign.currency)} recorded` : 'No received contributions yet'}</p></div><Badge variant="outline" className={campaign.published ? 'border-sage/40 text-sage-light' : 'border-gold/20 text-champagne/55'}>{campaign.published ? 'Published' : 'Private'}</Badge></div><div className="mt-3 flex flex-wrap gap-2"><Button size="sm" variant="outline" disabled={!canEdit} onClick={() => void patchCampaign(campaign,{published:!campaign.published})} className="border-gold/20 bg-transparent">{campaign.published ? 'Unpublish' : 'Publish'}</Button><Button size="sm" variant="outline" disabled={!canEdit} onClick={() => void patchCampaign(campaign,{invitationVisible:!campaign.invitationVisible})} className="border-gold/20 bg-transparent">Invitation: {campaign.invitationVisible ? 'On' : 'Off'}</Button><Button size="sm" variant="outline" disabled={!canEdit} onClick={() => void patchCampaign(campaign,{showRaised:!campaign.showRaised})} className="border-gold/20 bg-transparent">Progress: {campaign.showRaised ? 'Shown' : 'Hidden'}</Button><Button size="sm" variant="outline" disabled={!canEdit} onClick={() => void patchCampaign(campaign,{showTarget:!campaign.showTarget})} className="border-gold/20 bg-transparent">Target: {campaign.showTarget ? 'Shown' : 'Hidden'}</Button><Button size="sm" variant="outline" disabled={!canEdit} onClick={() => void patchCampaign(campaign,{showContributorRecognition:!campaign.showContributorRecognition})} className="border-gold/20 bg-transparent">Consented names: {campaign.showContributorRecognition ? 'Shown' : 'Private'}</Button></div><details className="mt-3 rounded-lg border border-gold/10"><summary className="cursor-pointer px-3 py-2 text-xs text-gold">Campaign details & publication window</summary><form onSubmit={(event)=>{event.preventDefault();const data=new FormData(event.currentTarget);void patchCampaign(campaign,{type:String(data.get('type')||campaign.type),title:String(data.get('title')||campaign.title),description:String(data.get('description')||''),targetAmount:data.get('targetAmount')?Number(data.get('targetAmount')):null,externalUrl:String(data.get('externalUrl')||''),ctaLabel:String(data.get('ctaLabel')||''),publicNote:String(data.get('publicNote')||''),publishFrom:String(data.get('publishFrom')||''),publishUntil:String(data.get('publishUntil')||'')})}} className="grid gap-2 border-t border-gold/10 p-3 sm:grid-cols-2"><select name="type" defaultValue={campaign.type} className="h-10 rounded-md border border-gold/20 bg-espresso px-2 text-xs">{Object.entries(CONTRIBUTION_CAMPAIGN_TYPE_LABELS).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><Input name="title" defaultValue={campaign.title} className="border-gold/20 bg-espresso/70"/><Input name="targetAmount" inputMode="decimal" defaultValue={campaign.targetAmount ?? ''} placeholder="Optional target" className="border-gold/20 bg-espresso/70"/><Input name="ctaLabel" defaultValue={campaign.ctaLabel ?? ''} placeholder="CTA label" className="border-gold/20 bg-espresso/70"/><Input name="externalUrl" defaultValue={campaign.externalUrl ?? ''} placeholder="HTTPS registry/payment link" className="border-gold/20 bg-espresso/70 sm:col-span-2"/><Textarea name="description" defaultValue={campaign.description ?? ''} placeholder="Public description" className="border-gold/20 bg-espresso/70 sm:col-span-2"/><Textarea name="publicNote" defaultValue={campaign.publicNote ?? ''} placeholder="Optional appreciative public note" className="border-gold/20 bg-espresso/70 sm:col-span-2"/><div><Label>Publish from</Label><Input name="publishFrom" type="datetime-local" defaultValue={campaign.publishFrom ? campaign.publishFrom.slice(0,16) : ''} className="mt-1 border-gold/20 bg-espresso/70"/></div><div><Label>Publish until</Label><Input name="publishUntil" type="datetime-local" defaultValue={campaign.publishUntil ? campaign.publishUntil.slice(0,16) : ''} className="mt-1 border-gold/20 bg-espresso/70"/></div><Button disabled={saving} className="bg-gold text-espresso sm:col-span-2">Save campaign details</Button></form></details></div>)}</div> : null}<div className="mt-4"><Button type="button" variant="outline" onClick={()=>void prepareGiftingShare()} className="border-gold/20 bg-transparent">Prepare public gifting link / QR</Button>{giftingShare && <div className="mt-3 grid gap-3 rounded-xl border border-gold/12 p-3 sm:grid-cols-[1fr_auto]"><div><p className="break-all text-xs text-gold">{giftingShare.url}</p><p className="mt-1 text-[10px] text-champagne/45">This opens the canonical public Registry section. Only published campaign fields are exposed.</p></div><img src={giftingShare.qr} alt="QR code for public gifting information" className="size-28 rounded bg-champagne p-1"/></div>}</div></div><form onSubmit={createCampaign} className="grid content-start gap-2 sm:grid-cols-2"><select aria-label="Campaign type" value={campaignForm.type} onChange={(e) => setCampaignForm((c) => ({...c,type:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm">{Object.entries(CONTRIBUTION_CAMPAIGN_TYPE_LABELS).map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select><Input required value={campaignForm.title} onChange={(e) => setCampaignForm((c) => ({...c,title:e.target.value}))} placeholder="Honeymoon fund" className="border-gold/20 bg-espresso/70" /><Input inputMode="decimal" value={campaignForm.targetAmount} onChange={(e) => setCampaignForm((c) => ({...c,targetAmount:e.target.value}))} placeholder="Optional target" className="border-gold/20 bg-espresso/70" /><Input value={campaignForm.description} onChange={(e) => setCampaignForm((c) => ({...c,description:e.target.value}))} placeholder="A short, appreciative note" className="border-gold/20 bg-espresso/70 sm:col-span-2" /><Input value={campaignForm.externalUrl} onChange={(e) => setCampaignForm((c) => ({...c,externalUrl:e.target.value}))} placeholder="Optional HTTPS registry/payment link" className="border-gold/20 bg-espresso/70 sm:col-span-2" /><Button disabled={saving || !canEdit} className="bg-gold text-espresso sm:col-span-2">Create private campaign</Button></form></div></Panel>
    </main>

    {addOpen && <div className="fixed inset-0 z-[100] flex items-end justify-center bg-black/70 p-2 backdrop-blur-sm sm:items-center"><button aria-label="Close add contribution" className="absolute inset-0" onClick={() => setAddOpen(false)} /><form onSubmit={addContribution} role="dialog" aria-modal="true" aria-label="Add contribution" className="relative z-10 max-h-[92dvh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-gold/25 bg-espresso p-4 shadow-2xl sm:p-5"><div className="flex items-start justify-between"><div><p className="text-[10px] uppercase tracking-[0.16em] text-gold">Add contribution</p><h2 className="mt-1 font-serif text-xl">Record the help, not accounting jargon</h2></div><Button type="button" variant="ghost" size="icon" onClick={() => setAddOpen(false)}><X className="size-4" /></Button></div><div className="mt-5 space-y-5">
      <fieldset><legend className="font-medium">1. Who contributed?</legend><div className="mt-2 grid gap-2 sm:grid-cols-2"><select aria-label="Choose existing contributor" value={form.contributorId} onChange={(e) => setForm((c) => ({...c,contributorId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">Add someone new</option>{workspace?.contributors.map((person) => <option key={person.id} value={person.id}>{person.displayName}</option>)}</select>{!form.contributorId && <Input required value={form.contributorName} onChange={(e) => setForm((c) => ({...c,contributorName:e.target.value}))} placeholder="Name or organisation" className="border-gold/20 bg-espresso/70" />}{!form.contributorId && <><select aria-label="Contributor type" value={form.contributorKind} onChange={(e) => setForm((c) => ({...c,contributorKind:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="individual">Individual</option><option value="family">Family / household</option><option value="organisation">Organisation / sponsor</option></select><Input type="email" value={form.email} onChange={(e) => setForm((c) => ({...c,email:e.target.value}))} placeholder="Email (optional)" className="border-gold/20 bg-espresso/70" /><Input value={form.phone} onChange={(e) => setForm((c) => ({...c,phone:e.target.value}))} placeholder="Phone (optional)" className="border-gold/20 bg-espresso/70" /><Input value={form.relationship} onChange={(e) => setForm((c) => ({...c,relationship:e.target.value}))} placeholder="Relationship, e.g. Bride's aunt" className="border-gold/20 bg-espresso/70" /><Input value={form.address} onChange={(e) => setForm((c) => ({...c,address:e.target.value}))} placeholder="Address (optional)" className="border-gold/20 bg-espresso/70" /><select aria-label="Preferred contributor contact" value={form.preferredContactMethod} onChange={(e) => setForm((c) => ({...c,preferredContactMethod:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">No contact preference</option><option value="email">Email</option><option value="phone">Phone</option><option value="other">Other</option></select><label className="flex items-start gap-2 rounded-lg border border-gold/15 p-3 text-xs text-champagne/60 sm:col-span-2"><Checkbox checked={form.publicRecognition} onCheckedChange={(checked) => setForm((c) => ({...c,publicRecognition:checked===true}))} /><span><strong className="text-champagne/80">Contributor is comfortable being publicly acknowledged.</strong><br/>This is private by default and does not automatically publish their name or amount.</span></label><select value={form.guestId} onChange={(e) => setForm((c) => ({...c,guestId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm sm:col-span-2"><option value="">Not linked to guest list</option>{workspace?.options.guests.map((guest) => <option key={guest.id} value={guest.id}>{guest.name}</option>)}</select></>}</div></fieldset>
      <fieldset><legend className="font-medium">2. What did they contribute?</legend><div className="mt-2 grid gap-2 sm:grid-cols-2"><select value={form.type} onChange={(e) => setForm((c) => ({...c,type:e.target.value as ContributionType,serviceEngagementId:''}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm">{Object.entries(CONTRIBUTION_TYPE_LABELS).map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select><Input required value={form.title} onChange={(e) => setForm((c) => ({...c,title:e.target.value}))} placeholder="What did they help with?" className="border-gold/20 bg-espresso/70" />{['CASH_TO_COUPLE','DIRECT_VENDOR_PAYMENT','HONEYMOON_GIFT'].includes(form.type) ? <Input required inputMode="decimal" value={form.amount} onChange={(e) => setForm((c) => ({...c,amount:e.target.value}))} placeholder="Amount" className="border-gold/20 bg-espresso/70" /> : <Input inputMode="decimal" value={form.estimatedValue} onChange={(e) => setForm((c) => ({...c,estimatedValue:e.target.value}))} placeholder="Estimated value (optional)" className="border-gold/20 bg-espresso/70" />}<Input value={form.currency} maxLength={3} onChange={(e) => setForm((c) => ({...c,currency:e.target.value.toUpperCase()}))} placeholder="USD" className="border-gold/20 bg-espresso/70" />{['GOODS_IN_KIND','SERVICE_IN_KIND','TIME_LABOUR'].includes(form.type) && <><Input inputMode="decimal" value={form.quantity} onChange={(e) => setForm((c) => ({...c,quantity:e.target.value}))} placeholder="Quantity (optional)" className="border-gold/20 bg-espresso/70" /><Input value={form.unit} onChange={(e) => setForm((c) => ({...c,unit:e.target.value}))} placeholder="Unit, e.g. crates, hours" className="border-gold/20 bg-espresso/70" /></>}</div></fieldset>
      <fieldset><legend className="font-medium">3. Where is it going?</legend><div className="mt-2 grid gap-2 sm:grid-cols-2"><select value={form.budgetItemId} onChange={(e) => { const item=workspace?.options.budgetItems.find((candidate)=>candidate.id===e.target.value); setForm((c)=>({...c,budgetItemId:e.target.value,serviceEngagementId:c.serviceEngagementId || item?.serviceEngagementId || ''})) }} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">Not allocated yet</option>{workspace?.options.budgetItems.map((item) => <option key={item.id} value={item.id}>{item.description}</option>)}</select>{form.type === 'DIRECT_VENDOR_PAYMENT' && <select required value={form.serviceEngagementId} onChange={(e) => setForm((c) => ({...c,serviceEngagementId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">Choose vendor service engagement</option>{workspace?.options.engagements.map((item) => <option key={item.id} value={item.id}>{item.vendor.name} — {item.serviceCategory}</option>)}</select>}<select value={form.campaignId} onChange={(e) => setForm((c) => ({...c,campaignId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">No honeymoon/campaign</option>{workspace?.campaigns.map((campaign) => <option key={campaign.id} value={campaign.id}>{campaign.title}</option>)}</select></div></fieldset>
      <fieldset><legend className="font-medium">4. What is the current state?</legend><div className="mt-2 grid gap-2 sm:grid-cols-2"><select value={form.status} onChange={(e) => setForm((c) => ({...c,status:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="PROMISED">Promised — not received yet</option><option value="RECEIVED">Received / paid / delivered</option></select>{form.status === 'PROMISED' && <Input type="date" value={form.expectedAt} onChange={(e)=>setForm((c)=>({...c,expectedAt:e.target.value}))} className="border-gold/20 bg-espresso/70" aria-label="Expected contribution date" />}{form.type === 'DIRECT_VENDOR_PAYMENT' && <Input value={form.paymentReference} onChange={(e) => setForm((c) => ({...c,paymentReference:e.target.value}))} placeholder="Payment reference (optional)" className="border-gold/20 bg-espresso/70" />}{form.type === 'DIRECT_VENDOR_PAYMENT' && form.budgetItemId && <label className="flex items-start gap-2 rounded-lg border border-gold/15 p-3 text-xs text-champagne/60 sm:col-span-2"><Checkbox checked={form.alreadyIncludedInBudgetPaid} onCheckedChange={(checked) => setForm((c) => ({...c,alreadyIncludedInBudgetPaid:checked===true}))} /><span><strong className="text-champagne/80">This payment is already included in the Budget “Paid” amount.</strong><br/>Check this for historical payments so Wewed records who funded it without adding the amount again.</span></label>}<Textarea value={form.notes} onChange={(e) => setForm((c) => ({...c,notes:e.target.value}))} placeholder="Optional context" className="border-gold/20 bg-espresso/70 sm:col-span-2" /></div></fieldset>
      <div className="rounded-xl border border-gold/15 bg-gold/5 p-3 text-xs leading-5 text-champagne/60">A promise is never counted as money received. Direct vendor payments remain real vendor payments with the contributor recorded as the funding source. In-kind values stay separate from cash paid.</div><Button disabled={saving} className="w-full bg-gold text-espresso hover:bg-gold-light">{saving ? <Loader2 className="size-4 animate-spin" /> : <HandHeart className="size-4" />}Save contribution</Button>
    </div></form></div>}

    {manage && <div className="fixed inset-0 z-[100] flex items-end justify-center bg-black/70 p-2 backdrop-blur-sm sm:items-center"><button aria-label="Close contribution details" className="absolute inset-0" onClick={() => setManage(null)} /><section role="dialog" aria-modal="true" aria-label="Manage contribution" className="relative z-10 max-h-[92dvh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-gold/25 bg-espresso p-4 shadow-2xl sm:p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] uppercase tracking-[0.16em] text-gold">{manage.contributor.displayName}</p><h2 className="mt-1 font-serif text-xl">{manage.title}</h2><p className="mt-1 text-xs text-champagne/50">{contributionTypeText(manage)} · {human(manage.fulfillmentState)}</p></div><Button variant="ghost" size="icon" onClick={() => setManage(null)}><X className="size-4" /></Button></div>{manage.type === 'DIRECT_VENDOR_PAYMENT' ? <div className="mt-5 grid gap-3 sm:grid-cols-3"><Panel className="p-3"><p className="text-[10px] uppercase text-champagne/40">Promised</p><p className="mt-1 font-serif text-xl">{money(manage.amount ?? 0,manage.currency)}</p></Panel><Panel className="p-3"><p className="text-[10px] uppercase text-champagne/40">Paid</p><p className="mt-1 font-serif text-xl">{money(manage.directVendorPaidAmount,manage.currency)}</p></Panel><Panel className="p-3"><p className="text-[10px] uppercase text-champagne/40">Remaining</p><p className="mt-1 font-serif text-xl">{money(manage.remainingAmount,manage.currency)}</p></Panel></div> : <div className="mt-5 grid gap-3 sm:grid-cols-2"><Panel className="p-3"><p className="text-[10px] uppercase text-champagne/40">Recorded value</p><p className="mt-1 font-serif text-xl">{manage.amount !== null ? money(manage.amount,manage.currency) : manage.estimatedValue !== null ? `${money(manage.estimatedValue,manage.estimatedValueCurrency || manage.currency)} est.` : 'Not valued'}</p></Panel><Panel className="p-3"><p className="text-[10px] uppercase text-champagne/40">Still available</p><p className="mt-1 font-serif text-xl">{money(manage.availableAmount,manage.currency)}</p><p className="mt-1 text-[10px] text-champagne/40">Verification: {human(manage.verificationState)}</p></Panel></div>}{canEdit && manage.commitmentState === 'PLEDGED' && manage.fulfillmentState === 'PENDING' && manage.type !== 'DIRECT_VENDOR_PAYMENT' && <Button onClick={() => void contributionAction('mark-received')} disabled={saving} className="mt-4 bg-gold text-espresso">Mark received / delivered</Button>}{canEdit && manage.availableAmount > 0 && <div className="mt-4 rounded-xl border border-gold/15 p-3"><h3 className="font-medium">Use some of this money</h3><p className="mt-1 text-xs text-champagne/50">This allocation records where received contribution money is intended to be used. It does not create a second payment.</p><div className="mt-2 grid gap-2 sm:grid-cols-[1fr_9rem_auto]"><select value={allocation.budgetItemId} onChange={(e) => setAllocation((c) => ({...c,budgetItemId:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="">Choose budget item</option>{workspace?.options.budgetItems.filter((item) => item.currency === manage.currency).map((item) => <option key={item.id} value={item.id}>{item.description}</option>)}</select><Input inputMode="decimal" value={allocation.amount} onChange={(e) => setAllocation((c) => ({...c,amount:e.target.value}))} placeholder="Amount" className="border-gold/20 bg-espresso/70" /><Button disabled={saving} onClick={() => void contributionAction('allocate',{budgetItemId:allocation.budgetItemId,amount:Number(allocation.amount)})} className="bg-gold text-espresso">Allocate</Button></div></div>}{canEdit && manage.type === 'DIRECT_VENDOR_PAYMENT' && manage.commitmentState === 'PLEDGED' && ['PENDING','PARTIALLY_RECEIVED'].includes(manage.fulfillmentState) && manage.remainingAmount > 0 && <div className="mt-4 rounded-xl border border-gold/15 p-3"><h3 className="font-medium">Record a vendor payment</h3><p className="mt-1 text-xs text-champagne/50">Enter only what the contributor actually paid now. Wewed creates a real vendor payment for that installment, updates Budget Paid by the same amount, and keeps the rest outstanding.</p><div className="mt-2 grid gap-2 sm:grid-cols-[9rem_1fr_auto]"><Input inputMode="decimal" value={directPaymentAmount} onChange={(e) => setDirectPaymentAmount(e.target.value)} placeholder={`Amount (max ${money(manage.remainingAmount,manage.currency)})`} className="border-gold/20 bg-espresso/70" /><Input value={directPaymentReference} onChange={(e) => setDirectPaymentReference(e.target.value)} placeholder="Payment reference (optional)" className="border-gold/20 bg-espresso/70" /><Button disabled={saving || !directPaymentAmount || Number(directPaymentAmount) <= 0 || Number(directPaymentAmount) > manage.remainingAmount} onClick={() => void contributionAction('mark-direct-paid',{amount:Number(directPaymentAmount),paymentReference:directPaymentReference})} className="bg-gold text-espresso">Record amount paid</Button></div></div>}<div className="mt-4 rounded-xl border border-gold/15 p-3"><div className="flex items-center justify-between gap-2"><div><h3 className="font-medium">Contribution details</h3><p className="text-xs text-champagne/45">Ordinary corrections stay editable; reconciled financial facts remain protected.</p></div>{canEdit && <Button size="sm" variant="outline" onClick={() => void saveContributionDetails()} className="border-gold/20 bg-transparent">Save details</Button>}</div><div className="mt-3 grid gap-2 sm:grid-cols-2"><Input disabled={!canEdit} value={contributionEdit.title} onChange={(e)=>setContributionEdit((c)=>({...c,title:e.target.value}))} placeholder="Contribution title" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} type="date" value={contributionEdit.expectedAt} onChange={(e)=>setContributionEdit((c)=>({...c,expectedAt:e.target.value}))} className="border-gold/20 bg-espresso/70"/>{manage.type === 'DIRECT_VENDOR_PAYMENT' && manage.fulfillmentState === 'PENDING' && <div><Label>Promised amount</Label><Input disabled={!canEdit} inputMode="decimal" value={contributionEdit.amount} onChange={(e)=>setContributionEdit((c)=>({...c,amount:e.target.value}))} placeholder="Promised amount" className="mt-1 border-gold/20 bg-espresso/70"/></div>}<Input disabled={!canEdit} inputMode="decimal" value={contributionEdit.estimatedValue} onChange={(e)=>setContributionEdit((c)=>({...c,estimatedValue:e.target.value}))} placeholder="Estimated value" className="border-gold/20 bg-espresso/70"/><div className="grid grid-cols-2 gap-2"><Input disabled={!canEdit} inputMode="decimal" value={contributionEdit.quantity} onChange={(e)=>setContributionEdit((c)=>({...c,quantity:e.target.value}))} placeholder="Quantity" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} value={contributionEdit.unit} onChange={(e)=>setContributionEdit((c)=>({...c,unit:e.target.value}))} placeholder="Unit" className="border-gold/20 bg-espresso/70"/></div><Textarea disabled={!canEdit} value={contributionEdit.description} onChange={(e)=>setContributionEdit((c)=>({...c,description:e.target.value}))} placeholder="Description" className="border-gold/20 bg-espresso/70 sm:col-span-2"/><Textarea disabled={!canEdit} value={contributionEdit.notes} onChange={(e)=>setContributionEdit((c)=>({...c,notes:e.target.value}))} placeholder="Private notes" className="border-gold/20 bg-espresso/70 sm:col-span-2"/></div></div>

      <div className="mt-4 rounded-xl border border-gold/15 p-3"><div className="flex items-start justify-between gap-3"><div><h3 className="font-medium">Contributor profile</h3><p className="text-xs text-champagne/45">Reusable identity and private contact record for this wedding.</p></div>{canEdit && <Button size="sm" variant="outline" onClick={() => void saveContributorProfile()} className="border-gold/20 bg-transparent">Save person</Button>}</div>{contributorProfile ? <><div className="mt-3 grid gap-2 sm:grid-cols-2"><Input disabled={!canEdit} value={contributorEdit.displayName} onChange={(e)=>setContributorEdit((c)=>({...c,displayName:e.target.value}))} placeholder="Display name" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} value={contributorEdit.legalName} onChange={(e)=>setContributorEdit((c)=>({...c,legalName:e.target.value}))} placeholder="Legal / organisation name" className="border-gold/20 bg-espresso/70"/><select disabled={!canEdit} value={contributorEdit.kind} onChange={(e)=>setContributorEdit((c)=>({...c,kind:e.target.value}))} className="h-10 rounded-md border border-gold/20 bg-espresso px-3 text-sm"><option value="individual">Individual</option><option value="family">Family / household</option><option value="organisation">Organisation / sponsor</option></select><Input disabled={!canEdit} value={contributorEdit.relationship} onChange={(e)=>setContributorEdit((c)=>({...c,relationship:e.target.value}))} placeholder="Relationship" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} type="email" value={contributorEdit.email} onChange={(e)=>setContributorEdit((c)=>({...c,email:e.target.value}))} placeholder="Email" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} value={contributorEdit.phone} onChange={(e)=>setContributorEdit((c)=>({...c,phone:e.target.value}))} placeholder="Phone" className="border-gold/20 bg-espresso/70"/><Input disabled={!canEdit} value={contributorEdit.address} onChange={(e)=>setContributorEdit((c)=>({...c,address:e.target.value}))} placeholder="Address" className="border-gold/20 bg-espresso/70 sm:col-span-2"/><Textarea disabled={!canEdit} value={contributorEdit.notes} onChange={(e)=>setContributorEdit((c)=>({...c,notes:e.target.value}))} placeholder="Private contributor notes" className="border-gold/20 bg-espresso/70 sm:col-span-2"/></div><div className="mt-3 flex flex-wrap gap-3 text-xs text-champagne/60"><label className="flex items-center gap-2"><Checkbox disabled={!canEdit} checked={contributorEdit.publicRecognition} onCheckedChange={(checked)=>setContributorEdit((c)=>({...c,publicRecognition:checked===true}))}/>Public acknowledgement allowed</label><label className="flex items-center gap-2"><Checkbox disabled={!canEdit} checked={contributorEdit.anonymousPublic} onCheckedChange={(checked)=>setContributorEdit((c)=>({...c,anonymousPublic:checked===true}))}/>Keep public identity anonymous</label></div>{contributorProfile.history?.length ? <div className="mt-4"><p className="text-[10px] uppercase tracking-[0.14em] text-gold">Contribution history</p><div className="mt-2 space-y-1">{contributorProfile.history.slice(0,6).map((entry)=><div key={entry.id} className="flex items-center justify-between gap-3 rounded-lg border border-gold/10 px-2.5 py-2 text-xs"><span className="truncate">{entry.title}</span><span className="shrink-0 text-champagne/45">{entry.amount !== null ? money(entry.amount,entry.currency) : entry.estimatedValue !== null ? `${money(entry.estimatedValue,entry.estimatedValueCurrency || entry.currency)} est.` : human(entry.fulfillmentState)}</span></div>)}</div></div> : <p className="mt-3 text-xs text-champagne/45">This is the contributor's first recorded item.</p>}</> : <p className="mt-3 text-xs text-champagne/45">Loading contributor profile…</p>}</div>

      <div className="mt-4 rounded-xl border border-gold/15 p-3"><div><h3 className="font-medium">Evidence in Wewed Vault</h3><p className="mt-1 text-xs text-champagne/50">Attach proof, receipts, letters or delivery confirmation. Files remain private and governed by Vault retention rules.</p></div>{canEdit && <div className="mt-3 flex flex-col gap-2 sm:flex-row"><Input type="file" onChange={(e)=>setEvidenceFile(e.target.files?.[0] ?? null)} className="border-gold/20 bg-espresso/70"/><Button disabled={saving || !evidenceFile} onClick={() => void attachEvidence()} className="bg-gold text-espresso">Attach evidence</Button></div>}<div className="mt-3 space-y-2">{evidence.length ? evidence.map((item)=><div key={item.id} className="flex items-center justify-between gap-2 rounded-lg border border-gold/10 p-2"><button type="button" onClick={() => void openEvidence(item)} className="min-w-0 truncate text-left text-xs text-gold hover:underline">{item.displayName}</button><div className="flex shrink-0 items-center gap-2"><span className="text-[10px] text-champagne/40">{item.scanState === 'content_validated' ? 'Ready' : 'Scanning'}</span>{canEdit && <Button size="sm" variant="ghost" onClick={() => void unlinkEvidence(item)} className="h-7 px-2 text-[10px] text-champagne/45">Unlink</Button>}</div></div>) : <p className="text-xs text-champagne/40">No evidence attached.</p>}</div></div>

      <div className="mt-4 rounded-xl border border-gold/15 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><div><h3 className="font-medium">AI drafting assistant</h3><p className="text-xs text-champagne/45">Drafts text only. Nothing is sent, marked paid, verified, or created until you choose an existing Wewed action.</p></div><div className="flex gap-2"><Button size="sm" variant="outline" disabled={saving} onClick={()=>void draftWithAi('draft-follow-up-task')} className="border-gold/20 bg-transparent">Draft task</Button><Button size="sm" variant="outline" disabled={saving} onClick={()=>void draftWithAi('draft-thank-you')} className="border-gold/20 bg-transparent">Draft thank-you</Button></div></div>{aiDraft && <div className="mt-3 rounded-lg border border-gold/10 bg-espresso/50 p-3"><p className="whitespace-pre-wrap text-xs leading-5 text-champagne/70">{aiDraft.text}</p><div className="mt-2 flex flex-wrap gap-2">{aiDraft.kind==='task' ? <Button size="sm" onClick={()=>{setTaskTitle(aiDraft.text);setAiDraft(null)}} className="bg-gold text-espresso">Use as task title</Button> : <Button size="sm" onClick={()=>{setNoteText(aiDraft.text);setAiDraft(null)}} className="bg-gold text-espresso">Use as thank-you draft in Notebook</Button>}<Button size="sm" variant="ghost" onClick={()=>setAiDraft(null)}>Discard</Button></div></div>}</div>

      <div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-gold/15 p-3"><h3 className="font-medium">Follow-up task</h3><Input value={taskTitle} onChange={(e) => setTaskTitle(e.target.value)} className="mt-2 border-gold/20 bg-espresso/70" /><Button size="sm" disabled={saving || !canCreateTasks} onClick={() => void contributionAction('create-task',{title:taskTitle})} className="mt-2 bg-gold text-espresso"><Plus className="size-3.5" />Create task</Button></div><div className="rounded-xl border border-gold/15 p-3"><h3 className="font-medium">Notebook context</h3><Textarea value={noteText} onChange={(e) => setNoteText(e.target.value)} placeholder="Capture a promise, detail or conversation…" className="mt-2 min-h-20 border-gold/20 bg-espresso/70" /><Button size="sm" variant="outline" disabled={saving || !noteText.trim()} onClick={() => void createNotebookNote()} className="mt-2 border-gold/20 bg-transparent"><NotebookPen className="size-3.5" />Save linked note</Button></div></div><div className="mt-4 flex flex-wrap items-center gap-2">{canEdit && ['RECEIVED','DELIVERED','PAID_DIRECT','COMPLETED'].includes(manage.fulfillmentState) && !['CONFIRMED_BY_USER','RECONCILED'].includes(manage.verificationState) && <Button variant="outline" disabled={saving} onClick={() => void contributionAction('mark-verified')} className="border-sage/30 bg-transparent text-sage-light">Confirm details checked</Button>}<Button variant="outline" disabled={saving || !canEdit || manage.thankYouState === 'SENT'} onClick={() => void contributionAction('mark-thanked')} className="border-gold/20 bg-transparent"><Sparkles className="size-4" />{manage.thankYouState === 'SENT' ? 'Thank-you sent' : 'Mark thank-you sent'}</Button>{manage.allocations.length > 0 && <span className="text-xs text-champagne/45">Allocated: {manage.allocations.map((item) => `${item.budgetItem.description} ${money(item.amount,item.currency)}`).join(' · ')}</span>}</div></section></div>}
  </div>
}
