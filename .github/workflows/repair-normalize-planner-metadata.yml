name: DEPRECATED - broad Prisma history repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Do not replay historical migrations
        run: |
          echo "::error::This workflow is retired because the live Wewed schema is ahead of the historical Prisma ledger."
          echo "Use 'Deploy deferred invitation handoff' for the isolated Android handoff migration."
          exit 1
