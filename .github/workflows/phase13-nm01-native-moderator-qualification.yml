name: Phase 13 NM01 Native Moderator Qualification

on:
  push:
    branches:
      - native-mobile/phase13-digital-invitation-convergence-nm01-20260925

permissions:
  contents: read

concurrency:
  group: phase13-nm01-native-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out exact head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}

      - name: Set up Java
        uses: actions/setup-java@7a445ee88d4e23b52c33d4b032a600e9c2a9d879
        with:
          distribution: temurin
          java-version: '17'

      - name: Run Android unit tests
        working-directory: apps/android
        run: ./gradlew testDebugUnitTest --no-daemon

  ios:
    runs-on: macos-latest
    timeout-minutes: 35
    steps:
      - name: Check out exact head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}

      - name: Run Swift package tests
        working-directory: apps/ios
        run: swift test
