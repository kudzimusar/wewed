name: Apply planner navigation stability repair

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/apply-planner-navigation-stability.py'
      - '.github/workflows/planner-navigation-stability.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull-request head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply reviewed navigation stability repair
        run: python scripts/apply-planner-navigation-stability.py

      - name: Commit product and test changes
        run: |
          if git diff --quiet; then
            echo 'Patch already applied.'
            exit 0
          fi
          git config user.name 'wewed-release-bot'
          git config user.email '77352591+kudzimusar@users.noreply.github.com'
          git add \
            src/components/wedding/planner-workspace-stage7.tsx \
            src/components/wedding/planner-portal.tsx \
            src/components/ui/dialog.tsx \
            src/lib/planner-complete-gap-closure.test.ts \
            tests/e2e/planner-data-workflows.spec.ts \
            tests/e2e/planner-deep-link-navigation.spec.ts \
            tests/e2e/planner-overlay-containment.spec.ts
          git commit -m 'fix(planner): stabilize URL state and scroll restoration'
          git push origin HEAD:${{ github.head_ref }}
